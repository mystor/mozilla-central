[package]
name = "random-crate"
version = "0.1.0"
authors = [
  "Nobody <nobody@mozilla.org>",
]

[lib]
crate-type = ["rlib"]

[dependencies]
bogus = { version = "0.1.0", path = "/path/to/somewhere" }
